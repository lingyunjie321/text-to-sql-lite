# Stage 1 第三轮正式候选审核报告（2026-08-01）

- 触发原因：比较器窄范围调整（v3，text/varchar/bpchar 归入同一字符串类型
  族），按规则重建冻结后重跑正式候选。
- 报告文件：`evaluation/reports/pagila_mvp_stage1_candidate3.json`
- 用例文件：`evaluation/cases/pagila_mvp_all_draft.jsonl`（全 draft 副本）
- baseline ID：`0b658f083b685cf93938689d109007a9916550e4c78d7a93aa12b17aaa5d1df4`
- 比较器：`stage1-comparator-v3`（仅放宽 text/varchar/bpchar 类型等价；
  列名、列顺序、字符串值、大小写、前导/中间空格、数字与日期类型检查均不
  放宽）
- 自动证据：`14/18` 通过；失败 `4` 条（002、008、009、014）
- 本轮未执行任何 approve/reject/verify；主 Gold 文件保持上一轮状态
  （14 verified / 4 draft）；Pagila 容器继续保留

## 一、18 条汇总（候选 2 → 候选 3）

| Case | 候选 2 | 候选 3 | 说明 |
|---|---|---|---|
| 001 | 通过 | 通过 | 一致 |
| 002 | 失败 | 失败（字段召回+列不一致） | 模型输出波动；诊断重跑曾通过；保留第一轮 verified |
| 003 | 通过 | 通过 | 一致（已 verified） |
| 004 | 通过 | 通过 | 一致 |
| 005 | 失败 | **通过** | v3 类型族修复生效（值归一化 + text/varchar/bpchar 等价） |
| 006 | 通过 | 通过 | 一致 |
| 007 | 通过 | 通过 | 一致 |
| 008 | 失败 | 失败（安全拒绝） | 模型输出不稳定，允许表配置正确；不 approve |
| 009 | 失败 | 失败（安全拒绝） | `\|\|` 白名单不放开；维持未通过 |
| 010 | 通过 | 通过 | 一致（已 verified） |
| 011 | 通过 | 通过 | 一致（已 verified） |
| 012 | 失败 | **通过** | 本轮模型引用了 `inventory.inventory_id`，字段召回与比较均通过 |
| 013 | 通过 | 通过 | 一致 |
| 014 | 通过 | **失败**（安全拒绝） | 模型输出波动（本轮越权被拦截）；诊断性质同 008 |
| 015 | 通过 | 通过 | 一致（预期安全拒绝） |
| 016 | 通过 | 通过 | 一致（预期安全拒绝） |
| 017 | 通过 | 通过 | 一致（预期安全拒绝） |
| 018 | 通过 | 通过 | 一致 |

## 二、失败 4 条说明

- PG-MVP-002：执行成功（82 行），但返回列与标准答案不一致，字段召回失败；
  模型输出波动（此前诊断重跑通过）。按决定保留第一轮 `verified`，不重新
  审核。
- PG-MVP-008：模型生成越权 SQL，被安全门拒绝（`PERMISSION_DENIED`，零执行）；
  两次正式候选均失败，诊断重跑通过仅证明题目可解，不作为正式依据。保持
  `draft`。
- PG-MVP-009：模型生成 SQL 使用 `||` 拼接（安全白名单外），被拒绝；维持
  未通过，不放宽白名单。
- PG-MVP-014：本轮模型生成越权 SQL 被安全门拒绝（此前两轮均通过），属模型
  输出波动；保持 `draft`，等待后续候选或决策。

## 三、v3 调整验证

- 新增单元测试：text/varchar/bpchar 两两等价、带尾部空格值仍归一化、
  varchar vs 数字、timestamptz vs text、内部 char vs text 仍严格拒绝。
- 单元与安全：`1181 passed`；真实 Pagila 集成：`78 passed / 9 skipped`。
- 005 在 v3 下从"列不一致"修复为 `COMPARATOR_MATCH`（真实数据验证通过）。

## 四、状态与下一步

- 主 Gold 文件：`14 verified`（001/002/003/004/006/007/010/011/013/014/015/
  016/017/018）`/ 4 draft`（005/008/009/012）；其中 014 已 verified 但候选 3
  失败，按"不撤销历史审核"原则保留。
- 候选 3 自动证据 `14/18`；失败 002（历史 verified，波动记录）、008/009/014
  （模型输出问题）、以及 002 的正式候选失败记录。
- 待用户确认候选 3 结果后，再决定是否对 005/012（本轮通过）执行审核更新。
