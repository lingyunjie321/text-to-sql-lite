# Invalidated Stage 10 evaluation history

The archived `pagila_mvp_stage10_candidate_17_of_18_invalidated.json`
report is retained only as historical failure evidence.

It is permanently invalid because the run did not start from the required
all-draft Gold file, its semantic metadata and evaluation baseline were not
the newly frozen versions, and its independent audit rejected every Case.
It cannot be loaded by the current report contract, reviewed, or used to
update Case status.

The archived run recorded 17 of 18 automated passes. The remaining stable
failure code was `EVALUATION_FIELD_RECALL_FAILED`. No result from that run
is carried into the resumed candidate evaluation.
