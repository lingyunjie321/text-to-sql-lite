# Text-to-SQL Agent 交付前评估报告

- 评估日期：2026-07-31
- 评估对象：`main` @ `a1d59ff`（工作区干净，与 origin/main 同步）
- 评估方式：规格文档对照 + 实跑测试套件（真实 Pagila 容器 + 临时只读角色）+ 代码静态审查
- 评估基线：`docs/Text-to-SQL项目复现规格.md`、`docs/Text-to-SQL测试与验收规格.md`、`evaluation/cases/pagila_mvp.jsonl`

---

## 总体结论

**❌ 未达到可交付状态。** 项目自我定位即为"工程预览，不是生产发布"（RELEASE.md），
本次实测结果与该定位一致：工程闭环完整、测试底盘扎实，但存在
**4 项安全回归、冻结失效、发布资格门禁未通过**等阻塞问题。

| 维度 | 结论 | 评级 |
|---|---|---|
| 1. 功能完整性 | MVP 闭环完整；发布资格未过；新旧功能均有半完成项 | 🟡 部分达标 |
| 2. 代码质量 | 工程卫生好；docstring 覆盖低、有巨型函数与复制粘贴 | 🟡 中等 |
| 3. 测试覆盖 | 1252 例中 1242 通过 / 10 失败；无覆盖率指标配置 | 🟡 未达标 |
| 4. 性能与稳定性 | 有界设计完善且经契约测试验证；无负载/容量证据 | 🟡 部分达标 |
| 5. 安全性 | SQL 安全门强健；最新提交引入 4 项安全测试回归 | 🔴 存在回归 |
| 6. 文档与部署 | 开发者文档齐备；无生产部署/CI/回滚方案 | 🔴 不达标 |

---

## 1. 功能完整性：🟡 部分达标

### 已实现并验证

- **MVP 纵向闭环完整**：PostgreSQL+Pagila 元数据、BM25 Schema Linking、
  OpenAI-compatible SQL 生成、SQLGlot 安全门、只读执行（30s/1000 行）、
  最多三次修复、LangGraph Workflow、FastAPI、Trace、离线评测工具。
- **Stage 1 工程能力已落地**：显式 ComplexityRouteNode（10 节点）、
  探测 K=20 / 物化 K=5/10/20、BM25+Embedding 双路召回、RRF k=60、
  可解释 Rerank、服务端三档模型路由、上下文裁剪。
- **最新提交（a1d59ff）新增**：多数据库插件架构（MySQL 924 行 /
  StarRocks 272 行 / 方言与注册表）、多数据源 DSL 配置、
  前后端对齐 Phase 1-4、前端设置页与结果增强组件。

### 未完成 / 半完成项（含影响评估）

| 项 | 状态 | 交付影响 |
|---|---|---|
| MVP Stage 10 发布资格 | `not_passed`：自动 12/18，审核 12 过 6 拒 | 🔴 高：发布质量门未过 |
| 18 条 Gold Case | 全部 `draft`（verified=0） | 🔴 高：无已验证质量基线 |
| Stage 1 三层门禁 | functional / integration / real_env 全 `false` | 🔴 高：契约迁移、mutation checks、独立审查、第二真实模型、真实索引验证、正式候选均未完成 |
| 请求级 Override | `ModelOverride`/`DatasourceOverride` 字段已声明且前端已发送，但**后端零接线**（application/bootstrap 无引用）——覆写静默不生效 | 🟠 中：功能半完成，前端配置是"假开关" |
| MySQL/StarRocks 连接器 | 代码已写但**无真实数据库契约测试**（Stage 4 门禁要求） | 🟠 中：SQLGlot 可解析 ≠ 真实兼容 |
| Stage 2/3/5 | 业务知识、Session/Memory、缓存导出治理均未开始 | 🟡 属路线图范围，非本次交付承诺 |

## 2. 代码质量：🟡 中等

- **规模**：`app/` 77 个 Python 文件 / 17,815 行；全仓 332 个跟踪文件。
- **优点**：无 TODO/FIXME 残留、无 print 调试残留、函数签名类型标注 100%、
  `compileall` / `pip check` 通过、依赖全量锁版本。
- **问题**：
  - docstring 覆盖率估计 **<25%**；`workflow/nodes/` 抽查文件为 0%，
    `connectors/mysql.py` 仅 17%。
  - 巨型函数：`link_schema` 657 行、`_generate_sql` 365 行、
    `build_trace_record` 272 行；5 个文件逼近/超过千行。
  - 复制粘贴：`metadata_queries_mysql.py` ↔ `metadata_queries_starrocks.py`
    非空行相似度 **74%**，应抽共用模板。
- **技术文档**：主规格 33KB、测试规格 20KB、11 篇 ADR、22 篇阶段设计/计划，
  开发者向文档齐备；`SchemaCandidate.schema` 字段遮蔽 pydantic 基类属性
  （产生 UserWarning）需清理。

## 3. 测试覆盖：🟡 未达标（存在回归）

### 实测结果（2026-07-31，本机复跑）

| 套件 | 结果 | 说明 |
|---|---|---|
| unit + security | **1154 passed / 7 failed**（3.1s） | 确定性测试 |
| integration（真实 Pagila） | **88 passed / 3 failed**（7.2s） | 临时只读角色，跑后已清理（残留 0） |
| 合计 | **1242 / 1252 通过（99.2%），10 失败** | 上快照 ec276df 为 1252 全过 |

### 失败清单与阻塞原因

| # | 失败用例 | 根因 |
|---|---|---|
| 1-3 | `test_model_routing_security` ×3 | `QueryRequest` 改 `extra="ignore"` 后，注入 `model`/`complexity`/`top_k` 不再抛 ValidationError |
| 4 | `test_api_permissions::test_request_cannot_inject_allowlist_or_dependencies` | 注入 allowlist/provider 从 422 退化为 **500** |
| 5 | `test_api_models[payload6]` | extra 字段不再被拒绝 |
| 6-7 | `test_api_application` ×2 | OpenAPI 端点集合与响应契约随对齐改动变化 |
| 8-10 | integration ×3（evaluation_runner、synthetic ×2） | 新提交改动受控代码后**未重新冻结**，`stage1 calibration freeze is invalid`（机制按设计工作） |

> ⚠️ 提交信息称 "7 pre-existing failures, 0 new failures introduced"，
> 但相对上一发布快照（ec276df，1252 全过）这 7 项**确为本次新引入的回归**，
> 表述有误导性。
>
> 另：仓库未配置覆盖率工具（无 coverage.py/阈值），无法给出覆盖率百分比；
> 但测试分层（unit/security/integration/evaluation 四层、103 个测试文件）
> 结构完备。

## 4. 性能与稳定性：🟡 部分达标

- **有界性设计完善且经测试验证**：30s statement timeout（含取消证明）、
  1000 行截断、API 总超时 120s、Workflow 32 步上限、连接池 1-4、
  pool timeout 5s、metadata/execute 剩余 deadline 下传。
- **无内存泄漏直接证据**，也无明显隐患：连接池有界、上下文裁剪、
  无全局累积状态；但未做长时运行/泄漏专项测试。
- **缺口（均属 Stage 5 范围，尚未开始）**：无负载测试、无故障注入、
  无并发/容量/p50/p95/p99 任何指标数据；同步 API + 单进程 + 池上限 4
  是已知吞吐瓶颈；LLM 调用是主要延迟来源（无实测延迟分布数据）。
- 结论：在当前"本地/评测边界"内稳定性可信；**不具备生产负载交付证据**。

## 5. 安全性：🔴 存在回归

### 保持强健的部分

- SQLGlot 安全门全套用例通过：单语句、只读、对象/函数 allowlist、
  危险 AST 拒绝（22 个安全测试文件，绝大多数通过）。
- 只读账号 + 只读事务双防线；恶意修复不绕过安全门；错误脱敏；
  Gold 隔离与防污染测试通过；`.env` 未被 git 跟踪；集成测试使用
  临时随机只读角色且跑后清零。
- 依赖全锁版本，`pip check` 通过（未跑 CVE 扫描）。

### 本次评估新确认的回归与风险

| 级别 | 问题 |
|---|---|
| 🔴 高 | `extra="ignore"` 使未知字段静默通过（4 项安全/契约测试失败），违反测试规格"客户端不能声明模型/复杂度/Top-K"；对齐方案文档将旧行为标注为"高危（会 422）"，属于**规格冲突未裁决**——按 AGENTS.md 应以测试规格为准 |
| 🔴 高 | 注入 `allowlist`/`provider` 字段返回 **500 而非 422**，暴露稳健性缺陷 |
| 🟠 中 | `ModelOverride` 设计允许客户端传 `base_url`/`api_key`，`DatasourceOverride` 允许传 `password`——当前未接线故无实际暴露面，**接线前必须做 SSRF/凭据边界安全评审** |
| 🟡 低 | 后端无认证（固定身份，仅限本地演示边界）；BFF 有 API Key 注入但后端自身无鉴权 |

## 6. 文档与部署：🔴 不达标

- **齐备**：开发者文档（规格/ADR/计划/对齐报告/README/RELEASE）；
  前端有应用内 help 页；`.env.example` 与配置组文档完整；
  Pagila 基础设施（compose.yaml + 镜像 sha256 锁定 + 幂等只读角色脚本）。
- **陈旧**：README 仍称"尚未包含开源许可证"（MIT LICENSE 已于 7-30 加入）；
  `HANDOFF.local.md` 描述的是 ec276df 快照，已落后 HEAD 两个提交。
- **缺失**：
  - 无生产部署路径（无 Dockerfile、无 ASGI server 固定、README 明示
    "尚不是生产服务"）；
  - **无 CI/CD**（无 .github/workflows 等任何配置）；
  - **无回滚/升级迁移操作文档**（仅在 Stage 5 规划中提及）；
  - 无独立最终用户手册；
  - 前端无测试、无 typecheck 脚本（仅 next lint）。
- **端到端验证状态**：后端集成测试经真实 Pagila 验证通过（除 3 项冻结失效）；
  前端 build 本次未验证；前后端联调的端到端证据不存在。

---

## 关键问题清单（按优先级）

### P0 — 交付阻塞

1. **安全契约回归**：`extra="ignore"` 与测试规格冲突。二选一裁决：
   (a) 恢复请求模型严格拒绝未知字段（推荐，符合安全规格）；
   (b) 正式修订测试规格与安全测试，并补充"忽略即安全"的论证与审计。
   同时修复注入字段返回 500 的稳健性缺陷。
2. **Stage 1 冻结失效**：a1d59ff 改动受控代码后未重新冻结，
   3 项集成测试失败。需重新建立冻结或回退变更。
3. **质量门未过**：18 条 Gold 全 draft、Stage 10 资格 not_passed、
   Stage 1 三层门禁全 false。交付前至少需完成一轮正式候选 + 独立审核。

### P1 — 上线前必须解决

4. **Override 半完成**：要么完成后端接线（先做 api_key/base_url/password
   的传输与 SSRF 安全评审），要么从契约中移除，消除"假开关"。
5. **多数据库插件补契约测试**：MySQL/StarRocks 需真实数据库契约测试，
   否则不得宣称 Stage 4 能力。
6. **文档同步**：更新 README 许可证表述、重写 HANDOFF、在对齐文档中
   记录安全规格裁决结论。

### P2 — 建议纳入下一迭代

7. 建立 CI（至少跑 unit+security 门禁，阻断回归合入）。
8. 补充覆盖率指标与阈值；清理 `SchemaCandidate.schema` 遮蔽告警。
9. 重构巨型函数与 mysql/starrocks 元数据查询复制粘贴；提升 docstring 覆盖。
10. 补充 Dockerfile / 生产运行说明 / 回滚预案；补前端 typecheck 与最小测试。
11. 规划负载与故障注入测试（Stage 5 前置）。

---

*本报告基于 2026-07-31 实测证据生成；测试过程创建的临时数据库角色已清理（残留 0）。*
