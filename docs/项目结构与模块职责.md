# 项目结构与模块职责

本文档展示当前仓库关键目录与职责，只列出理解和维护项目所需的文件，不展开 `node_modules`、构建产物、缓存文件或数据库二进制内容。

## 关键文件树

```text
text_to_sql/
  AGENTS.md                         # 项目约束与质量要求
  README.md                         # 项目首页和常用命令
  pyproject.toml                    # Python 包、依赖、pytest、ruff 配置
  workflow.yaml                     # 默认 Text-to-SQL 工作流配置
  .env.example                      # 本地数据库和配置路径示例
  configs/
    examples.yaml                   # 本地历史 Text-to-SQL 示例
  data/
    sqlite/
      demo.db                       # SQLite demo 数据库
  docs/
    整体架构.md                       # 当前实现整体架构
    文本转SQL工作流.md                 # 工作流流转、成功/修复/终止路径
    SQL生成过程代码追踪.md             # 从 API 到 SQL 执行的代码追踪
    完成度分析.md                     # 完成度、亮点、限制、后续方向
    文档维护规范.md                   # 文档维护规范
    面试演示场景.md                   # 面试演示脚本
    项目结构与模块职责.md             # 本文件
  examples/
    minimal_workflow.yaml           # 工作流核心的最小配置示例
  scripts/
    init_db.py                      # 从源码布局调用数据库初始化 CLI
    run_demo.py                     # 三条 Mock LLM 面试场景
  src/
    text_to_sql_demo/
      main.py                       # FastAPI app 和路由入口
      api/                          # API 请求模型、应用服务、响应序列化
      config/                       # workflow.yaml 的 Pydantic 配置模型和 loader
      workflow/                     # WorkflowState、BaseNode、Registry、Factory、Engine
      runtime/                      # 临时数据库和 light/strong 模型路由配置
      nodes/                        # 已注册的业务节点实现
      schema/                       # Schema introspection 和 schema linking
      retrieval/                    # 本地 SQL 示例加载与 Top-K 检索
      routing/                      # 查询复杂度分类和模型 alias 路由
      prompts/                      # PromptBuilder 和 prompt pruning 摘要
      llm/                          # LLMClient 协议与 MockLLMClient
      sql/                          # SQLGlot 方言、校验、错误模型
      execution/                    # SQLAlchemy 只读执行
      db/                           # SQLite demo schema 与种子数据
  tests/
    unit/                           # 配置、workflow、nodes、SQL、prompt 等单元测试
    integration/                    # API、demo 场景、修复闭环、CLI 集成测试
  frontend/
    package.json                    # Vite/React 脚本与依赖
    vite.config.ts                  # /api 代理到后端
    src/
      App.tsx                       # 前端主交互
      api/                          # API client 和响应类型
      adapters/                     # 后端响应到 UI view model 的适配
      components/                   # 查询输入、SQL 工作区、结果、Trace 展示
```

维护提示：新增顶层目录或移动入口文件时，需要同步更新本树、README 的文档导航和相关启动命令。

## 核心模块职责表

| 模块 | 入口文件 | 一句话职责 | 关键依赖 |
| --- | --- | --- | --- |
| FastAPI app | `src/text_to_sql_demo/main.py` | 注册健康检查、查询、运行记录、schema、只读 SQL 执行和方言转换接口。 | FastAPI、`TextToSQLApiService` |
| API service | `src/text_to_sql_demo/api/service.py` | 连接 API 与 workflow，负责配置、数据库初始化、schema 读取、engine 创建和响应序列化。 | `WorkflowEngine`、`NodeFactory`、`MockLLMClient` |
| Runtime config | `src/text_to_sql_demo/runtime/` | 保存前端创建的短生命周期运行配置，并解析为数据库 URL、目标方言、LLM client 和模型 profiles。 | `RuntimeConfigStore`、`RuntimeConfigResolver` |
| Workflow core | `src/text_to_sql_demo/workflow/engine.py` | 按配置执行节点并记录 Trace。 | `WorkflowConfig`、`NodeFactory`、`NodeResult` |
| Node registry/factory | `src/text_to_sql_demo/workflow/registry.py`、`factory.py` | 通过注册表按 node type 创建节点，避免 engine 硬编码具体节点。 | `BaseNode`、`NodeConfig` |
| Nodes | `src/text_to_sql_demo/nodes/__init__.py` | 导入节点模块触发 `@register_node` 注册。 | `SchemaLinkingNode` 等节点类 |
| Schema linking | `src/text_to_sql_demo/schema/linking.py` | 用表/列名称、中文同义词、描述和外键扩展选出相关 schema 子集。 | `DatabaseSchemaMetadata` |
| Example retrieval | `src/text_to_sql_demo/retrieval/examples.py` | 从 `configs/examples.yaml` 加载示例并按词法相似度和表重叠返回 Top-K。 | PyYAML、Pydantic |
| SQL generation | `src/text_to_sql_demo/nodes/sql_generation.py` | 在一个节点内完成复杂度分类、模型路由、prompt 构建和 LLM 调用。 | `PromptBuilder`、`ComplexityClassifier`、`LLMClient` |
| SQL validation | `src/text_to_sql_demo/sql/validator.py` | 用 SQLGlot 做方言解析、单语句、只读 SELECT、schema 引用和可选方言渲染。 | SQLGlot、schema metadata |
| SQL execution | `src/text_to_sql_demo/execution/sql_executor.py` | 用 SQLAlchemy 执行已校验 SQL 并归一化 columns/rows/duration。 | SQLAlchemy |
| Frontend app | `frontend/src/App.tsx` | 提供自然语言查询、运行配置、SQL 编辑执行、结果表格、修复提示和开发者信息。 | React、API client |

维护提示：如果模块职责发生漂移，例如 API service 开始构造 prompt、节点开始直接操作 HTTP 响应，应更新本表并考虑是否违反 AGENTS.md 的架构约束。

## 配置与数据文件

- `workflow.yaml`：真实工作流配置，声明 `nodes` 和 `edges`，模型 alias 为 `light`、`strong`。
- `configs/examples.yaml`：当前 `ExampleRetrievalNode` 默认读取的历史 SQL 示例。
- `.env.example`：包含 `DEMO_DATABASE_URL` 和 `TEXT_TO_SQL_WORKFLOW_CONFIG` 示例。
- `data/sqlite/demo.db`：可由 `python scripts/init_db.py --db-path data/sqlite/demo.db` 重新生成。
- `examples/minimal_workflow.yaml`：用于理解 workflow core 的最小配置示例，不是主 demo 配置。

维护提示：如果配置路径变化，尤其是 `examples_path`、database URL 或 workflow 文件名，必须同步更新 README、`docs/文本转SQL工作流.md` 和 demo 命令。

## 测试结构

后端测试按职责拆分：

- `tests/unit/workflow/`：registry、factory、engine 和架构约束。
- `tests/unit/nodes/`：schema linking、retrieval、mock LLM、生成节点。
- `tests/unit/sql/`：SQL 方言和校验。
- `tests/unit/prompts/`：prompt pruning 摘要。
- `tests/integration/`：API、CLI、demo 场景、修复闭环和数据库初始化。

前端测试覆盖：

- `frontend/src/App.test.tsx`：主交互、运行配置、Trace 展开、错误处理、SQL 编辑执行。
- `frontend/src/adapters/queryRunAdapter.test.ts`：API 响应到 UI view model 的适配。

维护提示：如果新增文档描述某条能力，优先找到对应测试；如果没有测试支撑，文档应明确写为限制或后续方向，而不是已实现能力。
